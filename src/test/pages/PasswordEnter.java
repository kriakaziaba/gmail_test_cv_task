package test.pages;

import org.openqa.selenium.remote.RemoteWebDriver;

public class PasswordEnter extends BasePage {
    public PasswordEnter(RemoteWebDriver driver) {
        super(driver);
    }
}
